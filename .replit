hidden = [".git","node_modules","dist/.vite",".cache",".next",".turbo","out"]

# (We will run from the shell to avoid duplicates)
# If you really want auto-run later, set: run = "npm run dev"

[nix]
channel = "stable-24_05"

[env]
PORT = "5000"
SKIP_VITE = "1"
NODE_OPTIONS = "--max-old-space-size=256"

[[ports]]
localPort = 5000
externalPort = 80
