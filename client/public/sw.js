// This file is now automatically generated by vite-plugin-pwa during build
// See vite.config.ts for the Workbox configuration
// The production service worker will be generated at build time with comprehensive caching strategies

// Development fallback
if (typeof self !== 'undefined') {
  self.addEventListener('install', (event) => {
    self.skipWaiting();
  });

  self.addEventListener('activate', (event) => {
    event.waitUntil(clients.claim());
  });

  self.addEventListener('fetch', (event) => {
    // Let the browser handle all requests in dev mode
    return;
  });
}

